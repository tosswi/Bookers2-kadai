
<!DOCTYPE html>
<html>
<H1>Editing Book</H1>
<%= form_for (@book) do |f|%>
<% if @book.errors.any? %>
<div id="erorr_explanation" >
	<ul>
		<%= @book.errors.count %>errors prohibited this obj from being saved:
		<% @book.errors.full_messages.each do |message| %>
		<%= message %>
		<% end %>
	</ul>
</div>
<% end %>

<h4>Title:</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %></p>

<%= f.submit 'Update Book' %>
<% end %>
<td><%= link_to "Show", book_path(@book.id) %></td>
<a href="/books" style="color:black">Back</a>
