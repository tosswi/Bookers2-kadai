
<h2>User info</h2>
<%= attachment_image_tag @user, :profile_image, size: '100x100', format: "jpeg", fallback: "no_image.jpg" %>

<h4>name<%= @user.name %></h4>

<h4>introduction<%= @user.introduction %></h4>

<%= link_to  edit_user_path(@user), class: 'btn btn-default btn-block' do
 %>
  <i class="glyphicon glyphicon-wrench"></i>
  <span></span>
<% end %>


<h2>New book</h2>
<%= form_for (@book) do |f| %>
<% if @book.errors.any? %>
<div id="erorr_explanation" >
	<ul>
		<%= @book.errors.count %>errors prohibited this obj from being saved:
		<% @book.errors.full_messages.each do |message| %>
		<%= message %>
		<% end %>
	</ul>
</div>
<% end %>
<label for="bookname">Title</label>
<div class="input" type="hidden"><%= f.text_field :title,placeholder:'タイトル' %></div>
<label for="bookname">Body</label>
<div class="input"><%= f.text_area :body, placeholder:'本文' %></div>
<%= f.submit 'Create Book' ,class: "btn btn-primary"%>
<% end %>