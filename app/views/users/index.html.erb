<% if flash[:notice] %>
  <p id="notice"><%= flash[:notice] %></p>
  <% end %>
<div class="container">
      <div class="row">
      	 <div class="col-xs-3">
<%= render'books/list',books:@book %>
</div>
<div class="col-xs-9">
<table class="table table-striped">
	<thead>
		<tr>
			<th>Image</th>
			<th>name</th>
			<th colspan="1"></th>
		</tr>
	</thead>
	<tbody>
		<h1>Users</h1>
		<% @users.each do |list| %>
		<tr>
			<td><%= attachment_image_tag list, :profile_image,size: '100x100', format:"jpeg",fallback:"no_image.jpg" %></td>
			<td><%= list.name %></td>
			<td><%= link_to "Show", user_path(list.id)%></td>
		</tr>
		<% end %>
		<br>
	</tbody>
</table>
</div>
</div>
</div>
